<UserControl x:Class="Enigma_Editor.Views.Hierarchy.HierarchyPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        Background="#1A1821"
        BorderBrush="#2B2836"
        >

        <DockPanel>

            <!-- HEADER -->
            <Grid DockPanel.Dock="Top">
                <TextBlock Text="Hierarchy"
                           Background="#1A1821"
                           Foreground="#E2E2E5"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="5" />

                <!-- ADD BUTTON -->
                <Button x:Name="AddButton"
                        Style="{StaticResource EditorButton}"
                        Width="20" Height="20"
                        HorizontalAlignment="Right"
                        Margin="0,0,5,0"
                        Click="AddButton_Click">
                    <Image Source="pack://application:,,,/assets/icons/Asset (11).png" Width="12" />
                    <Button.ContextMenu>
                        <ContextMenu>

                            <MenuItem Header="Entity" Click="CreateEntity_Click" />
                            <MenuItem Header="Camera" Click="CreateCamera_Click" />
                            <MenuItem Header="Box" Click="CreateBox_Click" />
                            <MenuItem Header="Circle" Click="CreateCircle_Click" />

                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>


            </Grid>


            <!-- TREEVIEW (uses external style) -->
            <TreeView
                x:Name="HierarchyTree"
                Background="#22202C"
                ItemsSource="{Binding HierarchyItems}"
                SelectedItemChanged="Hierarchy_Selected"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto">

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem"
                           BasedOn="{StaticResource HierarchyItemStyle}" />
                </TreeView.ItemContainerStyle>

            </TreeView>


        </DockPanel>
    </Border>
</UserControl>